// import React from 'react';
import PropTypes from 'prop-types';

const JobInvocationInputs = ({ data }) => 'Job invocation inputs';

JobInvocationInputs.propTypes = {
  data: PropTypes.object.isRequired,
};

export default JobInvocationInputs;
